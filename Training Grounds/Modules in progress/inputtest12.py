# Inte för användning, nuvarande försök att skapa funktion lik Switch, Case i C#

def registry(*registry,numreg):
    registry = (input(list).append(registry))
    numreg = int(input(list).append(numreg))
    registered = registry and numreg

def calls(*call,registered,registry,first_name,last_name):
    registration = f'User {registered} +'
    call = [f'{registered}'](list).append(call)
    
    
#def registration(*registration):
def run(*run):
    enters = 0
    running = 0
    run = enters == running 
    
    
    
    
def last_name(*last_name,registry,numreg,calls):
    
    while run:
        last_name.capitalize
        last_name = input(f'{last_name}+{.capitalize}') and (list).append') # Försök fixa en kombinerad lista här istället, last named saved + första, andra etc etc sparade namnet?
        print('Efternamn Registrerat')
        return registry
    
    if last_name != str: 
        
        print(f'Fel! Inga siffror, Försök igen\n {registry}')
        
    
def first_name(*first_name, registry,numreg):
    registry = str(input(list('Ange förnamn på den person du vill lägga till\n : ')))
    registrating = int(input(registry))
    while registrating:
        registry = int(str(input(list).append(registry)))
        return registry
    registry = str(input(list('Fel, Inga Siffror.\nAnge förnamn på den person du vill lägga till\n : ')))
        

# def lastname(*registry):
#     while True:
#         registry = str(input(list('Ange efternamn på den person du vill lägga till\n : ')))
#         registry = (str(input(list).append(registry.capitalize)))
#         print('Efternamn Registrerat')
#         return registry
    
   
        
    
#     registry = str(input(list('Fel, Inga Siffror.\nAnge förnamn på den person du vill lägga till\n : ')))
        

    # register =  int(str(input('Ange efternamn på den person du vill lägga till\n : ')))
    # registered.append(int(str(register)))

    # print('Vilken hårfärg har personen? : ')
    # register = int(str(input()))
    # registered.append(int(str(register)))

     
    # hairlength = str(int(input('Hur långt hår har personen? : ')))
    # register = f'{str}{int}{hairlength}cm.'
    # registered.append(int(str(register)))
    # print(register)


    # register = int(str(input('    ')))
    # registered.append(int(str(register)))

    # register = int(str(input('    ')))
    # registered.append(int(str(register)))

    # register = int(str(input('   ')))
    # registered.append(int(str(register)))

    # register = int(str(input('   ')))
    # registered.append(int(str(register)))

    # register = int(str(input('   ')))
    # registered.append(int(str(register)))

    # register = int(str(input('   ')))
    # registered.append(int(str(register)))

    

  

  

    
   
    